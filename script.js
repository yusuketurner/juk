const nav=document.querySelector('.nav');const menu=document.querySelector('.menu');let open=false;if(menu){menu.addEventListener('click',()=>{open=!open;document.body.classList.toggle('menu-open',open);if(nav)nav.classList.toggle('expanded',open)})}const navLinks=document.querySelector('.nav nav');if(navLinks&&!navLinks.querySelector('[href="blog.html"]')){navLinks.insertAdjacentHTML('beforeend','<a href="blog.html">ブログ</a><a href="contact.html">お問い合わせ</a>')}document.querySelectorAll('img[src^="assets/"]').forEach(img=>{img.src=img.src.replace(/\.webp$/i,'.svg')});const io=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('show');io.unobserve(e.target)}}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>io.observe(el));const parallax=[...document.querySelectorAll('.parallax')];const motion=[...document.querySelectorAll('.choice-card,.story-card.offset,.price-panel,.final-image')];let ticking=false;function move(){const y=window.scrollY;parallax.forEach(el=>{const r=el.getBoundingClientRect();if(r.bottom>0&&r.top<innerHeight){const speed=Number(el.dataset.speed||.08);const delta=(innerHeight/2-(r.top+r.height/2))*speed;el.style.transform=`translate3d(0,${delta}px,0)`}});motion.forEach((el,i)=>{const r=el.getBoundingClientRect();if(r.bottom>0&&r.top<innerHeight){const center=innerHeight/2;const progress=(r.top+r.height/2-center)/innerHeight;const drift=(i%2?-1:1)*progress*18;el.style.setProperty('--drift',`${drift}px`);el.style.setProperty('--tilt',`${progress*(i%2?-1:1)*1.2}deg`)}});if(nav)nav.style.boxShadow=y>30?'0 15px 40px #5c806d22':'0 12px 35px #5c806d1c';ticking=false}addEventListener('scroll',()=>{if(!ticking){requestAnimationFrame(move);ticking=true}},{passive:true});move();